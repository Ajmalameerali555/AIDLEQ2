import {Service} from '../../data/services';import {Link} from 'react-router-dom';import {Clock} from 'lucide-react';export function ServiceCard({s}:{s:Service}){return <article className="card overflow-hidden rounded-[28px]"><img loading="lazy" src={s.image} alt="" className="h-52 w-full object-cover"/><div className="p-6"><p className="text-sm font-bold text-gold-500">{s.category}</p><h3 className="font-heading text-2xl font-extrabold text-navy-950">{s.title}</h3><p className="mt-2 text-muted">{s.description}</p><div className="mt-4 flex items-center justify-between"><b>AED {s.priceFrom}+</b><span className="flex items-center gap-1 text-sm"><Clock size={15}/>{s.duration}</span></div><div className="mt-5 flex gap-3"><Link className="btn btn-gold py-2" to="/book">Book Now</Link><Link className="btn btn-navy py-2" to={`/services/${s.slug}`}>Details</Link></div></div></article>}
