import gsap from 'gsap';import {ScrollTrigger} from 'gsap/ScrollTrigger';gsap.registerPlugin(ScrollTrigger);export function reveal(scope:Element){if(matchMedia('(prefers-reduced-motion: reduce)').matches)return()=>{};const ctx=gsap.context(()=>{gsap.from('[data-reveal]',{y:34,opacity:0,duration:.8,stagger:.08,ease:'power3.out'});gsap.utils.toArray<HTMLElement>('[data-scroll-reveal]').forEach(el=>gsap.from(el,{scrollTrigger:{trigger:el,start:'top 82%',once:true},y:30,opacity:0,duration:.7,ease:'power3.out'}));},scope);return()=>ctx.revert()}
