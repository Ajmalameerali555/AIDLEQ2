import {useState} from 'react';export function BeforeAfterSlider({before,after,label}:{before:string;after:string;label:string}){const [v,setV]=useState(50);return <div className="relative overflow-hidden rounded-[32px] card" aria-label={label}><img src={after} alt={`${label} after`} className="h-80 w-full object-cover"/><div className="absolute inset-0 overflow-hidden" style={{width:`${v}%`}}><img src={before} alt={`${label} before`} className="h-80 w-[min(1180px,calc(100vw-32px))] max-w-none object-cover grayscale"/></div><input aria-label={`${label} comparison`} className="absolute inset-x-6 bottom-5 accent-gold-500" type="range" min="0" max="100" value={v} onChange={e=>setV(+e.target.value)}/></div>}
