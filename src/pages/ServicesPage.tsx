import {useMemo,useState} from 'react';import {categories,packages,services} from '../data/services';import {ServiceCard} from '../components/ui/ServiceCard';export function ServicesPage(){const [cat,setCat]=useState('All'),[q,setQ]=useState('');const list=useMemo(()=>services.filter(s=>(cat==='All'||s.category===cat)&&s.title.toLowerCase().includes(q.toLowerCase())),[cat,q]);return <main className="pt-28"><section className="section bg-navy-950 text-warm"><div className="container"><h1 className="font-heading text-6xl font-extrabold">Premium services</h1><p className="mt-4 max-w-2xl text-ivory/75">Search transparent AED pricing across maintenance, home cleaning and mobile car care.</p></div></section><section className="section"><div className="container"><div className="flex flex-wrap gap-3"><button onClick={()=>setCat('All')} className="btn btn-navy">All</button>{categories.map(c=><button onClick={()=>setCat(c)} className="btn btn-gold" key={c}>{c}</button>)}<input className="input max-w-sm" placeholder="Search services" value={q} onChange={e=>setQ(e.target.value)}/></div><div className="mt-10 grid gap-6 md:grid-cols-3">{list.map(s=><ServiceCard key={s.slug} s={s}/>)}</div><h2 className="mt-16 font-heading text-4xl font-extrabold">Frequently booked packages</h2><div className="mt-8 grid gap-6 md:grid-cols-3">{packages.map(p=><article className="card rounded-3xl p-7" key={p.title}><h3 className="text-2xl font-bold">{p.title}</h3><ul className="mt-4 space-y-2">{p.items.map(i=><li key={i}>✓ {i}</li>)}</ul><b className="mt-5 block text-gold-500">From AED {p.price}</b></article>)}</div></div></section></main>}
